---
name: page-review
description: 页面审查助手，对产品原型或页面（HTML/小程序/H5/Web后台等）进行系统性审查，覆盖信息完整性、逻辑正确性、交互惯例与可用性、视觉规范与信息层级、文案规范、全局一致性、边界与容错、合规与安全、功能合理性九个维度，输出结构化问题报告。触发词：页面审查、审查页面、页面走查、UI审查、原型审查、review页面、评审页面。
version: 1.0.0
agent_created: true
---

# 页面审查

对目标项目中的页面（原型 HTML、小程序、H5、Web 后台等）进行系统性审查，输出结构化问题报告。审查的依据是**外部客观标准**（行业认知 + 业界通用惯例），而非项目自定的 PRD。

## 总纲（审查依据原则）

1. **行业认知 / 业界习惯为主**：审查用"外部尺子"量项目，不用"项目自己写的 PRD"量自己（PRD 本身可能不合理）。判断"该有什么字段、交互合不合惯例"时，依据行业认知与主流产品惯例。
2. **仅配色、端尺寸等"无行业统一标准、纯品牌裁量"的强相关项**才读项目设计文件；这些具体设计值不写死在标准里。
3. **项目文件缺失时的兜底**：读取项目设计文件失败或文件不存在时，降级处理——先尝试从现有页面源码反推（扫描 CSS 变量/高频样式值），反推不可靠则改用内置通用默认值（只审相对关系：层级是否清晰、状态是否可区分、跨页是否一致，不卡具体色值/字号）。无论走哪级，报告开头必须标注实际采用的依据来源。

## 执行流程

1. **识别行业**（用于加载对应行业惯例包）：
   - 优先从用户请求中提取（如"审查这个宠物平台的页面"→ 宠物行业）；
   - 其次扫描项目文件推断（目录名、README、页面文案关键词、页面类型特征）；
   - 仍不确定或多候选时，反问用户确认。
2. **加载审查依据**：加载 `references/通用惯例.md`，并按识别出的行业加载 `references/行业惯例/` 下对应文件（无对应文件则用自身行业知识审查，并在报告中标注）。
3. **读项目文件（仅强相关项）**：仅取配色、端尺寸等强相关设计值。缺失时按总纲第 3 条兜底。
4. **逐页审查**：对每个页面执行「读取源码 → 渲染截图对照 → 按维度输出」。渲染截图方式见「审查方式」。
5. **生成报告**：按 `references/报告模板.md` 的结构输出，写入独立文件。
6. **测试自动任务：**这一条用来测试自动任务是否执行成功

## 审查方式

- **源码 + 渲染对照**：先读取页面源码，再用无头浏览器打开页面截图，将代码逻辑与渲染结果对照，避免只看源码或只看截图产生误判。
- **逐维度判断**：按 `references/审查维度.md` 中的九个维度逐一检查，每发现一处问题即记录：页面 + 维度标签 + 描述 + 修改建议 + 影响文件（含行号，尽量精确定位）。
- **产品视角与代码视角并重（防止漏审，务必执行）**：审查必须分两层，缺一不可。① 先「纯产品/用户视角」通读页面：**不读源码**，只凭渲染结果 + 行业惯例，把信息完整性、功能合理性、合规安全、视觉规范、边界容错等「仅靠看页面就能发现」的体验类问题全部抓全；② 再「代码逻辑对照」：读源码找数据错位、条件判断错误、跨页不一致等「只有读代码才能发现」的逻辑 bug。**严禁因深入代码细节而漏掉第①层的产品体验问题**（教训：曾专注读源码找 bug，漏掉星级分布缺失、无举报入口、筛选维度混杂、无排序、列表状态不闭环、辅助文字对比度不足等纯产品层问题）。
- **渲染环境注意**：无头浏览器可能不继承系统代理、无法加载外网 CDN 资源（如 Google Fonts），导致图标文本化。此类现象属于环境依赖，**不判为页面缺陷**，仅当怀疑真实部署环境也无法加载时才提示风险。

## 报告输出规范

- **独立文件**：每次审查生成一份新报告，不追加到旧报告。
- **结构固定（统一规范，勿变来变去）**：报告布局一律遵循 `references/报告模板.md`——头部信息块（审查日期/方式/范围/适用说明）→ `## 一、跨页面问题` → `## 二、按页面审查`（每个页面含 `**源码结构**` 一段说明）。每个问题固定三字段：**描述**、**修改建议**、**影响文件**（含行号）。禁止改用表格、禁止自行增减字段或调整层级。
- **只列问题**：仅记录确实存在的问题条目；"正常 / 无需修改 / 已复核通过"的条目一律不展示；复核确认是误判的条目直接删除。
- **不标注优先级**：不写 P0/P1/P2 标签，不设优先级汇总表，不写迭代顺序建议，由用户自行判断是否修改。
- **标注依据来源**：报告开头写明本次采用的行业与依据来源（如"按宠物行业惯例 + 通用标准审查"或"项目设计文件缺失，按通用默认值审查"）。
- **建议克制**：不主动建议"推荐流/猜你喜欢/热销推荐/相似推荐"类区块，除非用户明确要求。
- **图片类内容不审查显示合理性**：用户上传类内容（头像/晒图/评价图等）以及运营配置类图片（门店图/商品图/服务图/配图等），均不审查其显示是否合理（不评论图选得好不好、图文是否匹配、图片是否跨类型复用等）；但"缺少点击更换/上传入口""图片点击无预览"这类交互功能缺失仍属审查范围。

## 参考文件

- `references/审查维度.md`：九个维度的完整定义与判断点。
- `references/通用惯例.md`：不依赖项目文件的通用判断标准。
- `references/报告模板.md`：报告结构骨架。
- `references/行业惯例/`：各行业惯例清单（电商、宠物、医疗等），按需加载、可增删。
